import Layout from '../components/Layout';

const Signin = () => {
    return(
        <Layout>
            <h2>Signin Page</h2>
            <a href="/">Home</a>
        </Layout>
    );
};

export default Signin;